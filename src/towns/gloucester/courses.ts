// Gloucester race courses — authored on the real road graph with tools/make_course.mjs
// (see src/game/race.ts for the engine). Empty until the world is built; the
// ladder plan (same design laws as the other towns): a downtown/Boulevard dash
// past the Man at the Wheel, a middle run to the beaches, and the long ride
// home from Bearskin Neck in Rockport down Route 127.
import type { Course } from '../../game/race';

export const COURSES: Course[] = [];
