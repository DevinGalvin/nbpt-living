// rockport race courses — authored on the real road graph with
// tools/make_course.mjs (see src/game/race.ts for the engine).
import type { Course } from '../../game/race';

export const COURSES: Course[] = [];
