// charlestown race courses — authored on the real road graph with
// tools/make_course.mjs (see src/game/race.ts for the engine).
// Empty is a shippable state (Salisbury launched this way); the ladder comes
// next, and Charlestown has an obvious one: the hill, the Navy Yard, and a
// full loop of the peninsula.
import type { Course } from '../../game/race';

export const COURSES: Course[] = [];
